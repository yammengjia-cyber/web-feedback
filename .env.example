GITHUB_TOKEN=ghp_xxx
GITHUB_REPO_OWNER=your-github-username-or-org
GITHUB_REPO_NAME=your-repo-name
GITHUB_REPO_BRANCH=main
FEEDBACK_FILE_PATH=web-feedback-data/feedback.json
BLOB_READ_WRITE_TOKEN=vercel_blob_rw_xxx
# Optional: use "private" for a private Blob store (default), or "public" for a public one.
# BLOB_ACCESS=private
# Optional: max entries kept in feedback.json (default 2000). Sky UI still shows only latest 20.
# FEEDBACK_MAX_STORED=2000
